server:
  port: 9191

spring:
  application:
    name: spi-gateway
  cloud:
    gateway:
      routes:
        - id: login-service
          uri: lb://register-service
          predicates:
            - Path=/ecommerce/register/register/**
#          filters:
#            - name: CircuitBreaker
#              args:
#                name: register-service
#                fallbackuri: forward:/registerServiceFallBack
        - id: login-service
          uri: lb://login-service
          predicates:
            - Path=/login/**
#          filters:
#            - name: CircuitBreaker
#              args:
#                name: DEPARTMENT-SERVICE
#                fallbackuri: forward:/departmentServiceFallBack

